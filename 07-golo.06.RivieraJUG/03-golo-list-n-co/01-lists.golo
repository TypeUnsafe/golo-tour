module lists_demo

# = java.util.LinkedList
# head, tail, reverse(d), foreach, filter, find, reduce, join

function main = |args| {

  let books = list[
      "A Princess of Mars"
    , "The Gods of Mars"
    , "The Chessmen of Mars"
  ]
println(
  books: join("-")
)

  
}