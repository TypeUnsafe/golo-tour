module main


function main = |args| {
  
  let books = list[
      "A Princess of Mars"
    , "The Gods of Mars"
    , "The Chessmen of Mars"
  ]

  # filter sur contains
  # join
  # each
  # reverse

  println(books: reversed())
  

}