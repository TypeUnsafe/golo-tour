module loops

function main = |args| {
	
	# foreach
	let books = array[
		"A Princess of Mars",
		"The Gods of Mars",
		"The Chessmen of Mars"
	]

	foreach book in books { println(book) }

	# for, while

}